{
  "name": "delete_calendar_event",
  "description": "Delete a calendar event by ID. This is a destructive, irreversible action.",
  "permissions": ["network:read", "network:write"],
  "parameters": {
    "eventId": { "type": "string" }
  }
}
